This code does two things

1) It creates a array which is then filled with 100 random integers.

2) Iterates through the array and finds the largest increasing subsequence of numbers, which it then prints to the console.
